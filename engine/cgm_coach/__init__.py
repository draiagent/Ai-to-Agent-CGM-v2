"""CGM Coach 週報引擎（方案一骨架）。

模組：
    config       — 設定載入
    sheets       — Google Sheets 讀寫（骨架）
    libre        — LibreView CSV 解析（骨架）
    align        — T0 對齊、5 指標、資料排除規則（核心，已實作）
    flexibility  — CGM 共識指標面板（已實作）
    report       — 疊圖與 Markdown 週報（骨架）
    cli          — 進入點
"""

__version__ = "2.0.0"
